node_modules
# No trailing slash, and that is the whole of bc-slxm: a pattern ending in `/` matches
# **directories only**, and in a worktree `public/vendor` is a *symlink* to the main
# checkout's copy — which is what a session that wants to run the browser checks is told
# to make. git saw `?? public/vendor` there, and bin/deliver.js refuses a worktree with
# uncommitted changes in it, at the very end, after the suite has already passed.
# `node_modules` above has always been spelled this way, which is why only vendor bit.
public/vendor
*.log

# Per-session working notes, not part of the product. They also tend to quote
# machine-local details — the ntfy topic name, absolute paths, unrelated projects —
# which is exactly what you don't want in a repo you hand to someone else.
.claude/handoffs/

# Screenshots from `node scripts/shot.mjs`, written for an agent to Read and then
# forget. Every one of them is a picture of real beads — titles, workspaces, whatever
# was on screen — so they belong to the machine that took them and nowhere else.
.claude/shots/

# Soft-deleted worktrees, kept on disk so a finished session stays resumable, plus a
# .note saying what was retired and when. Local scratch like the two above — and the
# reason to ignore rather than merely leave untracked is that each one is a whole
# checkout, so a stray `git add .` in the main repo commits dozens of them at once.
.claude/worktrees-retired/

# Written by `node scripts/flowchart.mjs`, in a second, from lib/flowchart.js. Not
# committed on purpose, and the reason is the one bc-ec6 and bc-5ghk are both about:
# it is a 90 kB generated file that changes whenever the model or *any agent's
# foundation* does, so committing it would put a regenerate-and-resolve on every branch
# that touched lib/foundation.js for reasons of its own. The other half is that a
# checked-in copy has no way to say it is stale — docs/architecture.html still describes
# four agent kinds, and there have been five since lib/epicadvocate.js.
docs/flowchart.html

# Built and published by `npm run android` — 28 MB, and rebuilt from source.
public/beadcause.apk

# Android build output and machine-local SDK path.
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/
*.apk
*.keystore
*.jks
