# v19 — /session grew a composer

No new path — the code is in session.js and the styles in style.css, both already here —
but a phone holding v18 would cache a page that offers no way to answer a session, and
the one thing worse than not having the box is having it on the laptop and not on the
phone.
