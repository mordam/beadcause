# v18 — /session became a page of its own (v17), and then the sessions page it was folded inside went away and `/sessions` began serving the advocate console

Both changed this list, so both had to move the version — an installed worker would
otherwise go on handing the phone the cached /work.html shell, and a document deleted
from the repo would keep opening from the home screen for as long as the cache lived.
