# v95 — the space's settings become a page, and a v94 shell has a chip over an empty pane (bc-khoe.10)

The selected space's settings left `monitor.html` altogether. They are `/config` now — a
new document (`public/config.html`, `public/config.js`), a new alias pair in `serveStatic`
(`/config` and `/settings`), and the ninth pill on `public/viewbar.js`'s row — the
eighth is Releases, which took that place in v94. The
**Config** chip and its `<section id="config">` are gone from `monitor.html`; `monitor.js`
loses `spaceHtml`, `tri`, `repoTri`, `loadSpace`, `saveSpace` and every `data-space-*`
press branch with them, and draws the roster alone.

**A v94 `monitor.html` under a v95 `monitor.js` is the direction that shows.** That page
still has the Config chip and the empty section behind it, and the new script knows nothing
about either: `public/montabs.js` reads the chips out of the HTML, so the chip still swaps
to a pane holding the "Reading this space…" placeholder it ships with, and nothing ever
fills it. The settings are still reachable — the pill row is drawn by `viewbar.js`, which
is in the shell and updates with it, so **Config** is on the row and `/config` serves. So
the failure is a dead chip beside a live pill, not a lost screen.

**The reverse — a v95 `monitor.html` under a v94 `monitor.js` — is the harmless one.** The
old script looks for `#config`, finds nothing (`const cfg` is null and every write through
it was already guarded), and falls back to drawing the settings card at the top of the
roster where it lived before bc-me2b. That is the app as it was two days ago: correct, and
one screenful of switches above the first advocate.

**The pill row and the new document have to arrive together, which is the real pair.** A
shell holding any `viewbar.js` before this one has no Config pill and no `/config`
precached, so a phone on that shell reaches the settings only through whichever
`monitor.html` it also has. Both files are in `SHELL`, so one `install` brings the row, the
page and its script at once — which is the whole reason the four new paths (`/config`,
`/settings`, `/config.html`, `/config.js`) are precached rather than left to network-first.

**`config.html` loads `/hashroute.js` before `/viewbar.js`**, which is a v81 requirement
this page was written before: since bc-khoe.30.2 the row asks `route.viewOfPath` on boot
and a page that omits the tag throws before it draws. `/hashroute.js` and `/panes.js` are
already in `SHELL` from v81 and v83, so the tag costs nothing new to precache.

`lib/presence.js` needs no change: `config` has been one of its `VIEWS` since bc-me2b gave
the chip a `data-view`, and `public/mirror.js` already names it. What changed is only who
reports it — `montabs.js` did, and `config.js` does.

## The number

Written against v76, claimed **v77**, and landing as **v95** — the collision this scheme
exists to raise, and it has now raised it fifteen times. bc-rfnr.9.5 was holding `v77.md`
and bc-khoe.27.1 `v78.md` when this note was written; bc-bmry.5 (`v79.md`), bc-8t3b
(`v80.md`), bc-khoe.30.2 (`v81.md`), bc-khoe.23 (`v82.md`), bc-khoe.30.3 (`v83.md`),
bc-ka5y.15.3 (`v84.md`), bc-r2b5.2 (`v85.md`), bc-khoe.28 (`v86.md`), bc-khoe.26
(`v87.md`), bc-khoe.30.4 (`v88.md`), bc-khoe.27.2 (`v89.md`), bc-khoe.27.3 (`v90.md`),
bc-bmry.4 (`v91.md`), bc-khoe.30.5 (`v92.md`), bc-henk (`v93.md`) and bc-khoe.7
(`v94.md`) all reached `main` first. Each downmerge was an add/add conflict on the number this note was holding: the
note that landed first kept it, this one moved past all of them, and the `const` in
`public/sw.js` was set by hand to the highest note in this directory.

**The fourteenth was a straight race and it is the one worth recording**, because
both branches were right. bc-henk's advocate and this one both held `v93.md` at once, both
correctly — v93 was genuinely the next free number off `main` for each of us, and taking
the collision is the right failure. The alternative, dodging up to v94 to be polite, buys a
**gap**, and `test/swcache.mjs` refuses a gap exactly as it refuses a repeat: a branch
holding v94 over a `main` at v92 is red in its own tree until the v93 lands, and if that
branch is handed back rather than landed the red never clears and nothing the higher branch
does to itself will fix it. So the two advocates agreed by message to hold the same number
and let landing order decide. #438 landed first; this note renamed once, there — and again to v95 when bc-khoe.7 (#410)
took v94 while this branch was still waiting on Adam. That fifteenth was the ordinary
kind, not a race: v94 was free when this note claimed it and #410 simply landed first.

**One of them never showed up as a conflict at all**, and it is the case the filename
cannot report: bc-henk's merge advocate was holding `v84.md` unpushed on
`worktree-advocate-cards-henk` while this branch was resolving, so `main` had v83, both
trees had a free-looking v84, and both `const CACHE` lines would have merged in silence
because they would have been the identical string. It was caught by the two advocates
telling each other what they held, not by the tooling — and then bc-ka5y.15.3 landed v84
on `main` from a third direction and settled it. **The same silence happened again on the
v93 race and this time it was expected**: `docs/sw-cache/v93.md` conflicted loudly, because
the filename *is* the number, while `public/sw.js:42` merged without a murmur, because both
sides had written the identical string `beadcause-v93`. The loud half is the design; the
silent half is why the `const` is set by hand and re-read after every merge that says
nothing about it.

The pairs above are stated against **v94** rather than v76, because v94 is the shell a phone
will actually be holding. **The renumber was not free at v93, and that is the difference
from the twelve before it; at v94 it is free again, which is worth saying rather than
assuming.** bc-khoe.7 added a page and moved the deploy strip onto it, touching
`public/prs.js`, `public/index.html`, `public/style.css` and the row — and **neither
`public/monitor.html` nor `public/monitor.js`**, checked the same way the v93 claim was:
`git log ab0627d4..main -- public/monitor.html public/monitor.js` is empty. So every pair
below carries from v93 to v94 unchanged, and only the numbers moved. Of the sixteen notes
this one moved past, `monitor.html` — the
half of the pair this note is *about* — is still untouched by every one of them, which is
checked rather than asserted: `git log c7f6c3d9..main -- public/monitor.html` is empty. But
`public/monitor.js` is **not**: bc-henk (v93) rewrote 311 lines of it, making every advocate
a top-level card and an EpicAdvocate a card rather than a section inside the repo card. So
the v93 script a phone is holding is not the one this note was written against.

**Both directions survive it, and neither is weakened.** A v93 `monitor.html` still carries
the Config chip and the empty `<section id="config">` behind it, and the v94 script still
knows nothing about either, so that half is unchanged. The reverse half is the one that had
to be re-read rather than renumbered: the v93 `monitor.js` still holds `spaceHtml`, `tri`,
`repoTri`, `loadSpace` and `saveSpace` — this branch is what removes them — so it still
finds no `#config`, still falls back, and still draws the settings card at the top of the
roster. What changed is only *which* roster: bc-henk's, one card per advocate. A screenful
of switches above the first advocate card rather than above the first advocate section.
That is a different picture and the same failure, which is to say no failure at all.

The other two of the sixteen that went near this row, bc-khoe.30.2 (v81) and bc-khoe.30.3
(v83), moved the row's path table into `public/hashroute.js` and gave `index.html` a pane
per view; v88, v92 and the two ＋ notes are `index.html` and the panes behind it. bc-henk's
v93 pairs `monitor.js` with `public/style.css`, which this branch also touches — **comment
text only**, verified line by line, so it adds nothing to that pair. bc-mtdb did move
`monitor.html`, but only to drop its `<h1>`, which bc-khoe.10 puts back reading
**Advocates** — so a v94 page under a v95 script also differs by a heading, and that
difference is a word, not a failure.
