# Pin the public registry explicitly. A global ~/.npmrc pointing npm at a private
# feed (an Azure Artifacts or Nexus mirror, say) will otherwise be inherited here,
# and `npm install` 401s the moment that feed's token expires — with an error that
# looks like a network problem rather than a config one.
registry=https://registry.npmjs.org/
always-auth=false
